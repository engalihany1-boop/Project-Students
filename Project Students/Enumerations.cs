using System;
using System.Collections.Generic;
using System.Text;

namespace Project_Students
{
    public enum Department { CS, IT, IS, AI }
    public enum Grade { A_Plus, A, B_Plus, B, C, D, F }
    public enum StudentStatus { Active, Suspended, Graduated }
    public enum Semester { First, Second, Summer }
}
